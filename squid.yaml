manifestVersion: subsquid.io/v0.1
name: file-store-parquet-example
version: 1
description: |-
  A squid that indexes USDC Transfer events and saves the resulting data locally
build:

deploy:
  processor:
    cmd: [ "node", "lib/main" ]
